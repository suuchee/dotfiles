# Claude Code バックグラウンド実行の制限

`run_in_background: true` はユーザーの許可プロンプトをブロックするため、確認が発生しうる操作（git commit / git push / ファイル編集を含むサブエージェント等）では使わない。確認不要のコマンド（ビルド・テスト・調査系）にのみ使う。

**git commit は必ずフォアグラウンドで実行する。** バックグラウンド実行は TTY/stdin を持たず、承認プロンプトや pre-commit フック・push 認証等の対話入力でハングし、コミットが完了しないため。
